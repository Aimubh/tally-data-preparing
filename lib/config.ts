// App-level display config. The group name shown atop the sidebar. (No group
// entity in the schema — companies are the data; this is just the console title.)
export const GROUP_NAME = "Group MIS";
