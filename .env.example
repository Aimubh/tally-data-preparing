# Local, on-premise PostgreSQL (NOT Neon — this data stays on-premise).
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/group_mis?schema=public"
